<Window x:Class="Vistas.ABMCliente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ABMCliente" Height="300" Width="300" MinHeight="300" MinWidth="300"
         WindowStartupLocation="CenterScreen" Icon="/Vistas;component/Images/logoParking.png"
        Style="{StaticResource MyWindowDefault}">
    
    <Window.Resources>
        <ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
            <Style TargetType="Label" x:Key="MyTitle">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="Foreground" Value="Black" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="White" />
            </Style>
            
            <ControlTemplate x:Key="ErrorTemplate">
                <DockPanel LastChildFill="True">
                    <Border BorderBrush="Red" BorderThickness="3">
                        <AdornedElementPlaceholder />
                    </Border>
                </DockPanel>
            </ControlTemplate>

            <Style TargetType="TextBox">
                <Style.Triggers>
                    <Trigger Property="Validation.HasError" Value="true">
                        <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                        <Setter Property="ToolTipService.ShowDuration" Value="50000" />
                        <Setter Property="ToolTipService.Placement" Value="Bottom" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Label Content="ABM de Clientes" Height="36" HorizontalAlignment="Left" Margin="67,0,0,0" Name="label1" VerticalAlignment="Top" Style="{StaticResource MyTitle}" Foreground="#FF454444" />
        <Label Content="DNI" Height="28" HorizontalAlignment="Left" Margin="36,54,0,0" Name="dni" VerticalAlignment="Top" Width="54" />
        <TextBox Height="23" HorizontalAlignment="Left" Margin="107,57,0,0" Name="textDni" VerticalAlignment="Top" Width="140" Text="{Binding Path=Cli_ClienteDNI, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True }"  Validation.ErrorTemplate="{StaticResource ErrorTemplate}" LostFocus="textDni_LostFocus" />
        <Label Content="Apellido" Height="28" HorizontalAlignment="Left" Margin="36,88,0,0" Name="apellido" VerticalAlignment="Top" />
        <TextBox Height="23" HorizontalAlignment="Left" Margin="107,88,0,0" Name="textApellido" VerticalAlignment="Top" Width="140" Text="{Binding Path=Cli_Apellido, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Validation.ErrorTemplate="{StaticResource ErrorTemplate}"/>
        <Label Content="Nombre" Height="28" HorizontalAlignment="Left" Margin="36,122,0,0" Name="nombre" VerticalAlignment="Top" />
        <TextBox Height="23" HorizontalAlignment="Left" Margin="107,122,0,0" Name="textNombre" VerticalAlignment="Top" Width="140" Text="{Binding Path=Cli_Nombre, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Validation.ErrorTemplate="{StaticResource ErrorTemplate}"/>
        <Label Content="Telefono" Height="28" HorizontalAlignment="Left" Margin="36,156,0,0" Name="telefono" VerticalAlignment="Top" />
        <TextBox Height="23" HorizontalAlignment="Left" Margin="107,156,0,0" Name="textTelefono" VerticalAlignment="Top" Width="140" Text="{Binding Path=Cli_Telefono, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Validation.ErrorTemplate="{StaticResource ErrorTemplate}"/>
        <Button Content="Registrar" Height="30"  Name="btnRegistrar" Style="{StaticResource ButtonStyle}" Click="btnRegistrar_Click" VerticalAlignment="Top" Margin="36,200,145,0" />
        <Button Content="Modificar" Height="30"  Name="btnModificar" Style="{StaticResource ButtonStyle}" VerticalAlignment="Top" Margin="160,200,21,0" /> 
    </Grid>
</Window>
